# 세션 017 — 스토리 퀴즈 급수 표시 추가 (v5.24)

**날짜**: 2026-04-26  
**작업자**: Gemini CLI

---

## 주요 작업 목록

### v5.24 — 스토리 퀴즈 화면에 급수 정보 추가
- 스토리 퀴즈 모드에서 문제로 출제된 한자의 급수(예: 8급, 7급 등)를 카드 상단에 표시.
- 기존 학습 모드의 `card-hint` 스타일을 재사용하여 시각적 일관성 유지.
- 급수 정보를 통해 학습자가 난이도를 쉽게 파악할 수 있도록 UX 개선.

---

## 변경 사항 상세

1. **js/app.js**
   - `renderStoryQuiz()` 함수 내에 `${LEVEL_LABELS[card.level]}` 표시 코드 추가.
   - `APP_VERSION`을 `v5.23`에서 `v5.24`로 업데이트.

2. **sw.js**
   - `CACHE` 버전을 `hanja-v46`에서 `hanja-v47`로 업데이트.

3. **PROJECT.md**
   - 현재 버전 정보 갱신.

---

## 설계 결정사항

- **스타일 일관성**: 신규 클래스를 정의하는 대신 기존에 정의된 `.card-hint` 스타일을 활용하여 기존 앱 디자인 언어와 통일감을 줌.
