# 세션 014 — 단어 퀴즈 개선 + 성능 최적화 + UI 정리 (v5.11~v5.16)

**날짜**: 2026-04-25  
**작업자**: CoderCollie + Claude Sonnet 4.6  
**버전**: v5.10 → v5.16

---

## 작업 목록

### v5.11 — 퀴즈 피드백 응답속도 개선
- **Fix A**: 정답 피드백을 `app.innerHTML` 전체 교체 → 버튼 `classList` 직접 조작으로 변경 (즉각 반응)
- **Fix B**: SM2 state/accuracy를 메모리 캐시(`_sm2Cache`, `_accCache`)로 관리, 세션 중 JSON.parse 반복 제거

### v5.12 — 재시도 배지 → 카드 주황 테두리+글로우
- `badge-retry` 제거
- 재시도 카드에 `.card-retry` 클래스 → `border: 2px solid #f59e0b` + 글로우 효과

### v5.13 — 신규 카드 인트로 후 3장 뒤 즉시 퀴즈 재등장
- 인트로 확인 후 `state.queue.splice(insertAt, 0, card)` 로 3장 뒤에 삽입
- 기억이 남아있을 때 바로 테스트해 초기 기억 고정 효과

### v5.14 — 단어 퀴즈 SRS + 양쪽 훈음 표시
- `hanja_wq_state` 추가, 단어별 SM-2 스케줄링
- 답변 후 `先 먼저 선 · 頭 머리 두` 형식으로 훈음 표시 (이후 v5.15에서 방식 변경)
- `SM2._wqCache` 추가로 메모리 캐시 적용

### v5.15 — wq-kor 인플레이스 변환 + 딜레이 조정
- 답변 후 `선두` → `(먼저)선 (머리)두` — wq-kor 텍스트 자리에서 직접 변환 (새 요소 추가 없음)
- 피드백 딜레이: 정답 300→600ms, 오답 600→900ms

### v5.16 — wq-kor 훈음 스타일 개선
- `(먼저)` 부분: 작은 글씨 → 같은 크기, 색상 `#60a5fa` (옅은 파란색)

---

## 주요 피드백 메모

사용자로부터 "이해도 확인 없이 바로 구현하지 말 것" 피드백을 여러 차례 받음.
특히 UI/UX 변경 요청 시, 이해한 내용을 먼저 텍스트로 정리해서 확인받은 뒤 코드 작성 필요.
